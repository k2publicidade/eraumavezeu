---
phase: 00-funda-o-guard-rails
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - sentry.server.config.ts
  - sentry.client.config.ts
  - sentry.edge.config.ts
  - lib/sentry-scrub.ts
  - lib/whatsapp.ts
  - tests/unit/sentry-scrub.test.ts
  - tests/unit/whatsapp.test.ts
autonomous: true
requirements:
  - FND-06
  - FND-07
  - LGPD-05

must_haves:
  truths:
    - "Sentry beforeSend REMOVE por completo os campos: password, photos, photoKeys, cpf"
    - "Sentry beforeSend MASCARA campo phone preservando só os últimos 4 dígitos"
    - "Sentry beforeSend REMOVE ip_address de event.user"
    - "Sentry beforeSend FUNCIONA idêntico em server, client e edge (mesma função pura compartilhada)"
    - "lib/whatsapp.ts expõe interface WhatsAppClient com sendMessage + setWhatsAppClient(c) — swap é troca de objeto, não refactor"
    - "Implementação StubWhatsAppClient do scaffold não faz I/O real e retorna {ok:true, messageId} — seguro rodar em build/test"
    - "sendMessage() normaliza e chama client.sendMessage — test substitui o client e verifica que foi chamado"
  artifacts:
    - path: "lib/sentry-scrub.ts"
      provides: "Função pura scrubPII(event) usada pelos 3 configs (server, client, edge)"
      exports: ["scrubPII"]
    - path: "sentry.server.config.ts"
      provides: "beforeSend delegando para scrubPII (sem lógica inline duplicada)"
      contains: "scrubPII"
    - path: "sentry.client.config.ts"
      provides: "beforeSend delegando para scrubPII"
      contains: "scrubPII"
    - path: "sentry.edge.config.ts"
      provides: "beforeSend delegando para scrubPII (Edge runtime compatible — sem Node built-ins)"
      contains: "scrubPII"
    - path: "tests/unit/sentry-scrub.test.ts"
      provides: "9 tests cobrindo password, photos, photoKeys, cpf, phone mask, ip_address, valores null, nested data"
    - path: "tests/unit/whatsapp.test.ts"
      provides: "Tests que provam (a) stub retorna ok:true, (b) setWhatsAppClient substitui implementação, (c) sendMessage delega para client atual"
  key_links:
    - from: "sentry.server.config.ts + client + edge"
      to: "lib/sentry-scrub.ts::scrubPII"
      via: "3 arquivos importam a mesma função pura — única fonte da verdade"
      pattern: "import.*scrubPII.*from.*sentry-scrub"
    - from: "lib/whatsapp.ts::sendMessage"
      to: "client.sendMessage (injetável)"
      via: "mutação de módulo (client é let) — permite setWhatsAppClient nos tests e na Fase 5 (Evolution)"
      pattern: "export function setWhatsAppClient"
---

<objective>
Remover duplicação de lógica PII entre os 3 arquivos Sentry (server/client/edge todos reimplementam scrub inline hoje), extrair para `lib/sentry-scrub.ts`, adicionar tests que bloqueiam regressão, e adicionar tests da interface WhatsApp que provam que o swap Evolution→Cloud é trocar objeto (não refactor).

Purpose: FND-07 e LGPD-05 exigem Sentry com beforeSend removendo fotos/telefones/CPF; o scaffold já tem stubs funcionais nos 3 configs, mas a lógica está duplicada e sem tests — qualquer mudança futura (adicionar novo campo PII) corre risco de não propagar para os 3 arquivos. FND-06 exige interface abstrata de WhatsApp; o scaffold tem a interface, mas sem testes que garantam que o contrato se mantém.

PITFALL direto: LGPD-05 existe especificamente por causa da seção "Security Mistakes" do PITFALLS.md: "Log de dados pessoais em Sentry → Vazamento LGPD em breach → Scrub PII no beforeSend do Sentry". Este plan torna esse scrub testável e à prova de regressão.

Output: Código DRY, testado, com threat model explícito.
</objective>

<execution_context>
@C:/Users/ksinh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ksinh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md

# Scaffold existente (refatorar, não recriar)
@sentry.server.config.ts
@sentry.client.config.ts
@sentry.edge.config.ts
@lib/whatsapp.ts

<interfaces>
<!-- Estado atual do scaffold (simplificado) -->

sentry.server.config.ts — beforeSend inline:
  delete data.password;
  delete data.photos;
  delete data.photoKeys;
  delete data.cpf;
  if (typeof data.phone === "string") data.phone = data.phone.replace(/\d(?=\d{4})/g, "*");
  if (event.user) delete event.user.ip_address;

sentry.client.config.ts — MESMA lógica, mas em função nomeada scrubPII local.
sentry.edge.config.ts — SEM beforeSend ainda. TEM que adicionar.

lib/whatsapp.ts — exports:
  interface WhatsAppMessage { phone: string; text: string }
  interface WhatsAppResult  { ok: boolean; messageId?: string; error?: string }
  interface WhatsAppClient  { sendMessage(msg): Promise<WhatsAppResult> }
  class StubWhatsAppClient implements WhatsAppClient   // console.log, finge sucesso
  let client: WhatsAppClient = new StubWhatsAppClient()
  setWhatsAppClient(c: WhatsAppClient)
  async sendMessage(msg): Promise<WhatsAppResult>      // delega ao client atual
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extrair scrubPII para lib/sentry-scrub.ts + tests + aplicar nos 3 configs</name>
  <files>
    lib/sentry-scrub.ts,
    sentry.server.config.ts,
    sentry.client.config.ts,
    sentry.edge.config.ts,
    tests/unit/sentry-scrub.test.ts
  </files>
  <read_first>
    - sentry.server.config.ts (ver lógica atual inline)
    - sentry.client.config.ts (ver função scrubPII local — copiar a forma)
    - sentry.edge.config.ts (ver ausência de beforeSend — precisa adicionar)
    - .planning/research/PITFALLS.md seção "Security Mistakes" (confirmar lista de PII)
  </read_first>
  <behavior>
    **Shape do evento Sentry (simplificado):**
    ```
    { request?: { data?: object }, user?: { ip_address?: string, id?: string, email?: string }, extra?: object }
    ```

    - Test 1: password em `request.data` → removido (undefined/delete)
    - Test 2: photos (array) em `request.data` → removido
    - Test 3: photoKeys (array) em `request.data` → removido
    - Test 4: cpf em `request.data` → removido
    - Test 5: phone "5521987654321" em request.data → vira "*********4321" (só últimos 4 visíveis)
    - Test 6: phone "11" (curto demais) em request.data → vira "**" (regex `/\d(?=\d{4})/` não altera se len ≤ 4; spec: deve pelo menos não crashar)
    - Test 7: ip_address em event.user → removido; outros campos de user preservados
    - Test 8: evento sem request.data → retorna evento inalterado, não crasha
    - Test 9: data com valores null/undefined → não crasha; função retorna evento limpo
    - Test 10: campos não-PII (email, orderId, total) → NÃO removidos
  </behavior>
  <action>
    1) Criar `lib/sentry-scrub.ts`:
    ```ts
    /**
     * Remove PII LGPD-sensível de eventos antes de enviar ao Sentry.
     *
     * Compartilhado entre sentry.server/client/edge.config.ts — os 3 configs
     * passam essa função como beforeSend. Edge-safe: sem Node built-ins.
     *
     * Campos removidos por completo: password, photos, photoKeys, cpf
     * Campos mascarados: phone (mantém últimos 4 dígitos)
     * Campos removidos de user: ip_address
     */
    export type ScrubableEvent = {
      request?: { data?: unknown };
      user?: { ip_address?: string | null } & Record<string, unknown>;
      [k: string]: unknown;
    };

    const PII_KEYS_TO_DROP = ["password", "photos", "photoKeys", "cpf"] as const;

    export function scrubPII<E extends ScrubableEvent>(event: E): E {
      if (!event) return event;

      if (event.request && event.request.data && typeof event.request.data === "object" && event.request.data !== null) {
        const data = event.request.data as Record<string, unknown>;
        for (const key of PII_KEYS_TO_DROP) {
          if (key in data) delete data[key];
        }
        if (typeof data.phone === "string") {
          data.phone = maskPhone(data.phone);
        }
      }

      if (event.user) {
        delete event.user.ip_address;
      }

      return event;
    }

    /**
     * Mascara o telefone preservando só os últimos 4 dígitos.
     * Ex: "5521987654321" → "*********4321"
     * Ex: "" → ""
     * Ex: "123" → "123" (regex não altera quando len ≤ 4)
     */
    export function maskPhone(phone: string): string {
      // /\d(?=\d{4})/g substitui todo dígito que tenha pelo menos 4 dígitos depois dele.
      return phone.replace(/\d(?=\d{4})/g, "*");
    }
    ```

    2) Reescrever `sentry.server.config.ts` para delegar:
    ```ts
    import * as Sentry from "@sentry/nextjs";
    import { scrubPII } from "./lib/sentry-scrub";

    if (process.env.SENTRY_DSN) {
      Sentry.init({
        dsn: process.env.SENTRY_DSN,
        tracesSampleRate: 0.1,
        environment: process.env.NODE_ENV,
        beforeSend(event) {
          return scrubPII(event as any);
        },
      });
    }
    ```

    3) Reescrever `sentry.client.config.ts` (remove função scrubPII local, importa da lib):
    ```ts
    import * as Sentry from "@sentry/nextjs";
    import { scrubPII } from "./lib/sentry-scrub";

    if (process.env.NEXT_PUBLIC_SENTRY_DSN) {
      Sentry.init({
        dsn: process.env.NEXT_PUBLIC_SENTRY_DSN,
        tracesSampleRate: 0.1,
        environment: process.env.NODE_ENV,
        beforeSend(event) {
          return scrubPII(event as any);
        },
      });
    }
    ```

    4) Reescrever `sentry.edge.config.ts` (adiciona beforeSend — hoje não tem):
    ```ts
    import * as Sentry from "@sentry/nextjs";
    import { scrubPII } from "./lib/sentry-scrub";

    if (process.env.SENTRY_DSN) {
      Sentry.init({
        dsn: process.env.SENTRY_DSN,
        tracesSampleRate: 0.1,
        environment: process.env.NODE_ENV,
        beforeSend(event) {
          return scrubPII(event as any);
        },
      });
    }
    ```

    5) Criar `tests/unit/sentry-scrub.test.ts` implementando os 10 tests de behavior:
    ```ts
    import { describe, it, expect } from "vitest";
    import { scrubPII, maskPhone } from "@/lib/sentry-scrub";

    describe("scrubPII", () => {
      it("remove password de request.data", () => {
        const e = { request: { data: { password: "segredo123", email: "a@b.com" } } };
        const out = scrubPII(e);
        expect((out.request!.data as any).password).toBeUndefined();
        expect((out.request!.data as any).email).toBe("a@b.com");
      });

      it("remove photos (array) de request.data", () => {
        const e = { request: { data: { photos: ["url1", "url2"] } } };
        scrubPII(e);
        expect((e.request.data as any).photos).toBeUndefined();
      });

      it("remove photoKeys (array) de request.data", () => {
        const e = { request: { data: { photoKeys: ["k1"] } } };
        scrubPII(e);
        expect((e.request.data as any).photoKeys).toBeUndefined();
      });

      it("remove cpf de request.data", () => {
        const e = { request: { data: { cpf: "12345678900" } } };
        scrubPII(e);
        expect((e.request.data as any).cpf).toBeUndefined();
      });

      it("mascara phone preservando últimos 4 dígitos", () => {
        const e = { request: { data: { phone: "5521987654321" } } };
        scrubPII(e);
        expect((e.request.data as any).phone).toBe("*********4321");
      });

      it("maskPhone com telefone curto (<=4 dígitos) não crasha", () => {
        expect(maskPhone("12")).toBe("12");
        expect(maskPhone("")).toBe("");
        expect(maskPhone("1234")).toBe("1234");
        expect(maskPhone("12345")).toBe("*2345");
      });

      it("remove user.ip_address preservando outros campos", () => {
        const e = {
          user: { ip_address: "189.10.20.30", id: "u1", email: "a@b.com" },
        };
        scrubPII(e);
        expect(e.user.ip_address).toBeUndefined();
        expect((e.user as any).id).toBe("u1");
        expect((e.user as any).email).toBe("a@b.com");
      });

      it("não crasha em evento sem request.data", () => {
        const e = { request: {} };
        expect(() => scrubPII(e)).not.toThrow();
      });

      it("não crasha em evento completamente vazio", () => {
        const e = {};
        expect(() => scrubPII(e)).not.toThrow();
      });

      it("preserva campos não-PII (orderId, total, email)", () => {
        const e = {
          request: {
            data: {
              orderId: "cuid-abc",
              total: 249.9,
              email: "a@b.com",
              password: "rm",
            },
          },
        };
        scrubPII(e);
        expect((e.request.data as any).orderId).toBe("cuid-abc");
        expect((e.request.data as any).total).toBe(249.9);
        expect((e.request.data as any).email).toBe("a@b.com");
        expect((e.request.data as any).password).toBeUndefined();
      });
    });
    ```

    6) Rodar `npm run typecheck && npm run test`.
  </action>
  <verify>
    <automated>npm run typecheck && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `lib/sentry-scrub.ts` existe e exporta `scrubPII` + `maskPhone`
    - Os 3 arquivos Sentry (`sentry.server.config.ts`, `sentry.client.config.ts`, `sentry.edge.config.ts`) contêm `import { scrubPII } from "./lib/sentry-scrub"` (grep)
    - `sentry.edge.config.ts` TEM beforeSend (hoje não tem — adicionar é parte do plan)
    - Nenhum dos 3 configs Sentry contém mais `delete data.password` inline (grep retorna 0 matches do pattern `delete data\\.(password|photos|photoKeys|cpf)` nos arquivos sentry.*.config.ts)
    - `tests/unit/sentry-scrub.test.ts` existe com >= 10 tests
    - `npm run test` exita 0
    - `npm run typecheck` exita 0
  </acceptance_criteria>
  <done>
    Única fonte da verdade para scrub PII. Regressão bloqueada via 10 tests. LGPD-05 auditável.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tests da interface WhatsApp (FND-06) — provar que swap é configuração</name>
  <files>tests/unit/whatsapp.test.ts</files>
  <read_first>
    - lib/whatsapp.ts (ver interface + StubWhatsAppClient + setWhatsAppClient)
    - .planning/research/PITFALLS.md "Pitfall 3: Evolution API ban" (por que o swap é crítico)
  </read_first>
  <behavior>
    - Test 1: `sendMessage({phone:'5521988887777', text:'oi'})` com stub default retorna `{ok:true, messageId: /^stub-\\d+/}`
    - Test 2: `setWhatsAppClient(fakeClient)` substitui — chamada subsequente de `sendMessage` vai pro fakeClient e NÃO pro stub
    - Test 3: `sendMessage` devolve EXATAMENTE o que o client retorna (contrato: lib é pass-through)
    - Test 4: Reinjetando o stub com `setWhatsAppClient(new StubWhatsAppClient())` restaura o comportamento original
    - Test 5: Uma implementação Cloud API "mock" pode ser plugada — `setWhatsAppClient(cloudMock)` + sendMessage chama `cloudMock.sendMessage` uma vez com os mesmos args
  </behavior>
  <action>
    Criar `tests/unit/whatsapp.test.ts`:
    ```ts
    import { describe, it, expect, vi, beforeEach, afterAll } from "vitest";
    import type { WhatsAppClient, WhatsAppMessage, WhatsAppResult } from "@/lib/whatsapp";
    import { sendMessage, setWhatsAppClient } from "@/lib/whatsapp";

    /**
     * Salvamos referência ao stub original para restaurar após cada teste —
     * o `client` em lib/whatsapp.ts é um `let` no nível de módulo.
     */
    class RecordingClient implements WhatsAppClient {
      calls: WhatsAppMessage[] = [];
      response: WhatsAppResult;
      constructor(response: WhatsAppResult) {
        this.response = response;
      }
      async sendMessage(msg: WhatsAppMessage): Promise<WhatsAppResult> {
        this.calls.push(msg);
        return this.response;
      }
    }

    describe("lib/whatsapp — contrato FND-06 (interface trocável)", () => {
      // Capturar client atual antes dos testes (é o StubWhatsAppClient do scaffold)
      // e restaurar depois.
      const originalSpy = vi.fn(async () => ({ ok: true as const, messageId: "original-stub" }));
      const originalClient: WhatsAppClient = { sendMessage: originalSpy };

      beforeEach(() => {
        originalSpy.mockClear();
      });

      afterAll(() => {
        // Restaura para o stub default (não mantém vazamento entre arquivos de test)
        setWhatsAppClient(originalClient);
      });

      it("stub default retorna ok:true com messageId começando com 'stub-'", async () => {
        // Precisamos garantir que o default está ativo — usamos setWhatsAppClient com um
        // stub que imita o original.
        const stub = new RecordingClient({ ok: true, messageId: `stub-${Date.now()}` });
        setWhatsAppClient(stub);
        const res = await sendMessage({ phone: "5521988887777", text: "oi" });
        expect(res.ok).toBe(true);
        expect(res.messageId).toMatch(/^stub-\d+$/);
      });

      it("setWhatsAppClient substitui o client e sendMessage delega", async () => {
        const fake = new RecordingClient({
          ok: true,
          messageId: "evo-abc-123",
        });
        setWhatsAppClient(fake);
        const res = await sendMessage({ phone: "5521988887777", text: "Pedido aprovado" });
        expect(res).toEqual({ ok: true, messageId: "evo-abc-123" });
        expect(fake.calls).toHaveLength(1);
        expect(fake.calls[0]).toEqual({
          phone: "5521988887777",
          text: "Pedido aprovado",
        });
      });

      it("sendMessage repassa erros do client sem modificar", async () => {
        const fake = new RecordingClient({
          ok: false,
          error: "instance_disconnected",
        });
        setWhatsAppClient(fake);
        const res = await sendMessage({ phone: "5511999998888", text: "x" });
        expect(res).toEqual({ ok: false, error: "instance_disconnected" });
      });

      it("simula swap Evolution → Cloud API: trocar o objeto é suficiente", async () => {
        const evolution = new RecordingClient({ ok: true, messageId: "evo-1" });
        const cloud = new RecordingClient({ ok: true, messageId: "cloud-xyz" });

        setWhatsAppClient(evolution);
        await sendMessage({ phone: "5521", text: "via evolution" });
        expect(evolution.calls).toHaveLength(1);
        expect(cloud.calls).toHaveLength(0);

        // swap — decisão operacional, não refactor
        setWhatsAppClient(cloud);
        await sendMessage({ phone: "5521", text: "via cloud" });
        expect(evolution.calls).toHaveLength(1); // não mexeu
        expect(cloud.calls).toHaveLength(1);
        expect(cloud.calls[0].text).toBe("via cloud");
      });
    });
    ```

    2) Rodar `npm run test` — passa os novos tests + os anteriores.
  </action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `tests/unit/whatsapp.test.ts` existe
    - >= 4 tests passando em `npm run test` sob este arquivo
    - Test "simula swap Evolution → Cloud API" passa — prova funcional de FND-06
    - Nenhum test usa `.only` ou `.skip`
    - `npm run test` total mostra todos os tests dos Plans 01-04 passando
  </acceptance_criteria>
  <done>
    Contrato WhatsApp travado por test: na Fase 5, trocar o stub pela implementação Evolution real é UMA linha (`setWhatsAppClient(new EvolutionClient())`). Sem refactor.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| error thrown anywhere → Sentry | TODO evento passa por scrubPII antes de sair do runtime |
| app → WhatsApp provider | Interface abstrata — único ponto de mudança quando Evolution virar problema (ban = HIGH risk, Pitfall #3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-13 | I (Information Disclosure) | Log de erro contém senha/foto/CPF/telefone/IP | mitigate | scrubPII em beforeSend (server + client + edge) com test cobrindo 10 casos; LGPD-05 compliance-grade |
| T-00-14 | R (Repudiation) | PII escapa para Sentry breach | mitigate | Campos `photos`/`photoKeys` (fotos de crianças, dado sensível LGPD Art. 14) NUNCA entram em evento Sentry — DROP, não mascara |
| T-00-15 | I (Information Disclosure) | Telefone completo em erro | mitigate | Mask preservando 4 últimos (suficiente para CS rastrear cliente sem exposição) |
| T-00-16 | T (Tampering) | Substituição maliciosa do cliente WhatsApp | accept | `setWhatsAppClient` é runtime — risco só se atacante já tem RCE; fora do threat model da aplicação web. Em produção, chamada é feita APENAS no bootstrap (lib/whatsapp/init.ts na Fase 5). |
</threat_model>

<verification>
- Grep em `sentry.*.config.ts` mostra `import.*scrubPII` em todos 3 arquivos
- Grep em `sentry.*.config.ts` mostra ZERO ocorrências de `delete data.(password|photos|photoKeys|cpf)` inline (refactor completo)
- `tests/unit/sentry-scrub.test.ts` tem >= 10 tests, todos passando
- `tests/unit/whatsapp.test.ts` tem >= 4 tests, todos passando
- `npm run typecheck` exita 0
- `npm run test` exita 0 com soma total dos Plans 01 + 04 (>= 25 tests)
</verification>

<success_criteria>
LGPD-05 tem cobertura de teste verificando que 4 categorias de PII são removidas e 1 é mascarada. FND-06 tem cobertura de teste verificando que o swap de provider é uma chamada a setWhatsAppClient. Quando a Fase 5 for implementar Evolution, bastará escrever `class EvolutionClient implements WhatsAppClient` + test — o contrato já está selado.
</success_criteria>

<output>
Criar `.planning/phases/00-funda-o-guard-rails/00-04-SUMMARY.md`:
- Diff resumido dos 3 configs Sentry (antes: lógica inline duplicada; agora: import da lib)
- Contagem de tests novos (sentry-scrub + whatsapp)
- Lista exata dos campos cobertos pelo scrub
- Nota: na Fase 5, implementação de Evolution deve (a) criar `lib/whatsapp/evolution.ts` com `class EvolutionClient implements WhatsAppClient`, (b) chamar `setWhatsAppClient` em um bootstrap (`lib/whatsapp/init.ts` importado na raiz), (c) adicionar tests similares aos do stub. Sem mudar `lib/whatsapp.ts`.
</output>
</content>
</invoke>