---
phase: 00-funda-o-guard-rails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - tsconfig.json
  - .gitignore
  - app/globals.css
  - tests/setup.ts
  - tests/smoke/scaffold.test.ts
autonomous: true
requirements:
  - FND-01

must_haves:
  truths:
    - "pnpm install roda sem erros e popula node_modules com todas as deps"
    - "pnpm typecheck roda em modo strict e exita 0"
    - "pnpm build roda sem erros (Next 14 + Tailwind 3.4)"
    - "pnpm test roda Vitest e exita 0"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest config com alias @/* e ambiente node"
      contains: "defineConfig"
    - path: "tests/setup.ts"
      provides: "Vitest setup file"
    - path: "tests/smoke/scaffold.test.ts"
      provides: "Smoke test que valida a presença da stack mínima"
    - path: "package.json"
      provides: "Scripts test/test:watch + devDeps vitest"
      contains: "\"test\":"
    - path: ".gitignore"
      provides: "Ignora .env*, node_modules, .next, coverage, dist"
      contains: ".env.local"
  key_links:
    - from: "vitest.config.ts"
      to: "tsconfig.json paths"
      via: "alias '@' aponta para raiz do projeto"
      pattern: "resolve.*alias.*@"
    - from: "package.json"
      to: "vitest"
      via: "script test roda vitest run"
      pattern: "\"test\":.*\"vitest run\""
---

<objective>
Validar que o scaffold Next 14 + TypeScript estrito + Tailwind 3.4 (commit 59eb5e4) compila e roda, adicionar framework de testes Vitest (necessário para os Plans 03 e 04), e garantir que o `.gitignore` protege credenciais antes de qualquer `.env.local` ser criado.

Purpose: FND-01 exige que `pnpm dev` rode Next 14 com TypeScript estrito e Tailwind sem erros. O scaffold já tem todas as deps (Next 14.2.21, React 18.3.1, Tailwind 3.4.17, TS 5.7.2 strict). Falta instalar, validar o build, e adicionar Vitest — que é pré-requisito para os tests de Sentry PII scrub (Plan 04) e guards (Plan 03).
Output: Projeto com tests unitários rodando, gitignore protegendo .env.local, typecheck/build validados.
</objective>

<execution_context>
@C:/Users/ksinh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ksinh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md

# Scaffold atual (já commitado em 59eb5e4 — apenas validar)
@package.json
@tsconfig.json
@tailwind.config.ts
@next.config.mjs
@app/layout.tsx
@app/page.tsx
@app/globals.css

<interfaces>
<!-- Stack fixada conforme STACK.md. NUNCA subir para React 19 ou Tailwind 4. -->
<!-- Versões atuais no package.json: -->

next@14.2.21
react@18.3.1 / react-dom@18.3.1      # PINNED — react-pageflip quebra em 19
typescript@5.7.2                      # strict: true
tailwindcss@3.4.17                    # v4 quebra shadcn/ui
@prisma/client@6.1.0 + prisma@6.1.0
next-auth@5.0.0-beta.25 + @auth/prisma-adapter@2.7.4
@sentry/nextjs@8.47.0
sharp@0.33.5
uploadthing@7.4.4 + @uploadthing/react@7.1.4
mercadopago@2.3.0
resend@4.0.1 + @react-email/components@0.0.32
zustand@5.0.2
zod@3.24.1
react-hook-form@7.54.2 + @hookform/resolvers@3.9.1
bcryptjs@2.4.3 + @types/bcryptjs@2.4.6
clsx@2.1.1 + tailwind-merge@2.5.5 + lucide-react@0.469.0
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar deps, criar .gitignore e validar typecheck/build</name>
  <files>.gitignore, package-lock.json (gerado), node_modules/ (gerado)</files>
  <read_first>
    - package.json (ver versões pinnedas antes de rodar install)
    - tsconfig.json (confirmar strict:true antes de typecheck)
    - .env.example (saber quais arquivos NÃO podem ir para git)
  </read_first>
  <action>
    1) Criar `.gitignore` na raiz do projeto com EXATAMENTE este conteúdo (Windows-safe, cobre Next 14 + Prisma + Vitest + Sentry):
    ```
    # deps
    node_modules/
    .pnp
    .pnp.js

    # next
    .next/
    out/
    build/

    # env — NUNCA COMMITAR
    .env
    .env.local
    .env.*.local
    .env.production
    .env.development

    # logs
    npm-debug.log*
    yarn-debug.log*
    yarn-error.log*
    pnpm-debug.log*

    # OS
    .DS_Store
    Thumbs.db

    # IDE
    .idea/
    .vscode/*
    !.vscode/settings.json
    !.vscode/extensions.json

    # test / coverage
    coverage/
    *.lcov
    .nyc_output/

    # turbo
    .turbo/

    # prisma
    prisma/*.db
    prisma/*.db-journal

    # sentry
    .sentryclirc

    # vercel
    .vercel/

    # misc
    *.tsbuildinfo
    next-env.d.ts
    ```
    2) Rodar `npm install` (NÃO usar pnpm mesmo que CLAUDE.md sugira — package.json não tem `packageManager` definido, npm é o default e o package-lock já virá com npm).
    3) Rodar `npx prisma generate` (obrigatório — caso contrário `@prisma/client` não existe fisicamente e o typecheck quebra em lib/db.ts, prisma/seed.ts, lib/auth.ts).
    4) Rodar `npm run typecheck` — DEVE exitar 0 em modo strict. Se falhar, reportar erros e parar (NÃO mascarar com `any` ou `// @ts-ignore`).
    5) Rodar `npm run build` — DEVE compilar sem erros. Next precisa de `DATABASE_URL` em build em alguns casos? Não — o scaffold atual não tem rotas que consomem DB em build time, então build tem que passar mesmo sem `.env.local`. Se falhar por falta de env var, reportar exatamente qual.
  </action>
  <verify>
    <automated>test -f .gitignore && grep -q ".env.local" .gitignore && test -d node_modules && test -d node_modules/@prisma/client && npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` existe e contém a linha exata `.env.local` (grep passa)
    - `node_modules/` existe após `npm install`
    - `node_modules/@prisma/client/` existe (prisma generate rodou)
    - `npx tsc --noEmit` (npm run typecheck) exita 0
    - `npm run build` exita 0 e imprime rota `/` no summary de build
    - `.next/` foi criado após build
    - Arquivo `.env` / `.env.local` NÃO está trackeado pelo git (verificar com `git check-ignore .env.local` → output `.env.local`)
  </acceptance_criteria>
  <done>
    Stack base compilável, strict mode validado, credenciais protegidas por .gitignore.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Instalar Vitest e criar smoke tests da stack</name>
  <files>vitest.config.ts, tests/setup.ts, tests/smoke/scaffold.test.ts, package.json</files>
  <read_first>
    - package.json (ver scripts atuais antes de editar)
    - tsconfig.json (copiar paths `@/*` para vitest.config)
    - tailwind.config.ts (ver tokens que a smoke test vai validar)
    - lib/utils.ts (cn + formatBRL + slugify — já existem, usar em tests)
  </read_first>
  <behavior>
    - Test 1: `cn("a", "b")` retorna "a b" (utils.ts já existe, validar que import via alias `@/lib/utils` funciona)
    - Test 2: `formatBRL(249.9)` retorna string contendo "R$" e "249,90" (valida Intl pt-BR)
    - Test 3: `slugify("Era Uma Vez Eu!")` retorna "era-uma-vez-eu"
    - Test 4: tailwind.config.ts exporta objeto com `theme.extend.colors.primary.DEFAULT === "#D97B2B"` (proteção contra regressão de paleta)
    - Test 5: package.json tem `"next": "14.2.21"` exato (proteção contra upgrade acidental para Next 15 / React 19)
    - Test 6: package.json tem `"react": "18.3.1"` exato
    - Test 7: package.json tem `"tailwindcss": "^3.4.17"` (v3, NÃO v4)
  </behavior>
  <action>
    1) Instalar devDeps exatas: `npm install -D vitest@^2.1.8 @vitest/ui@^2.1.8 jsdom@^25.0.1`.
       - Vitest 2.x é compatível com Vite 5 + Node 20. NÃO usar 3.x (instabilidade).
       - jsdom é precaução para caso algum test futuro precise de DOM; environment default aqui é `node`.
    2) Criar `vitest.config.ts` na raiz com EXATAMENTE:
    ```ts
    import { defineConfig } from "vitest/config";
    import path from "node:path";

    export default defineConfig({
      resolve: {
        alias: {
          "@": path.resolve(__dirname, "."),
        },
      },
      test: {
        environment: "node",
        globals: true,
        setupFiles: ["./tests/setup.ts"],
        include: ["tests/**/*.test.ts", "tests/**/*.test.tsx"],
        coverage: {
          provider: "v8",
          reporter: ["text", "html"],
          exclude: [
            "node_modules/",
            ".next/",
            "tests/",
            "prisma/",
            "**/*.config.*",
            "**/types/**",
          ],
        },
      },
    });
    ```
    3) Criar `tests/setup.ts` com conteúdo mínimo (placeholder — plans futuros adicionam mocks):
    ```ts
    // Vitest global setup — executado antes de cada arquivo de teste.
    // Extensão: quando adicionar mocks globais (ex: mock do Prisma, vars de env para Sentry),
    // colocá-los aqui ou em tests/setup/*.ts importados daqui.
    ```
    4) Criar `tests/smoke/scaffold.test.ts` implementando os 7 tests de behavior acima:
    ```ts
    import { describe, it, expect } from "vitest";
    import { readFileSync } from "node:fs";
    import path from "node:path";
    import { cn, formatBRL, slugify } from "@/lib/utils";
    import tailwindConfig from "@/tailwind.config";

    describe("scaffold smoke", () => {
      it("cn combina classes via clsx + tailwind-merge", () => {
        expect(cn("a", "b")).toBe("a b");
        expect(cn("p-2", "p-4")).toBe("p-4"); // tailwind-merge resolve conflito
      });

      it("formatBRL produz string com R$ e vírgula decimal", () => {
        const out = formatBRL(249.9);
        expect(out).toContain("R$");
        expect(out).toContain("249,90");
      });

      it("slugify normaliza PT-BR com acentos", () => {
        expect(slugify("Era Uma Vez Eu!")).toBe("era-uma-vez-eu");
        expect(slugify("São João")).toBe("sao-joao");
      });

      it("tailwind.config preserva paleta primary/secondary", () => {
        const colors = (tailwindConfig.theme as any).extend.colors;
        expect(colors.primary.DEFAULT).toBe("#D97B2B");
        expect(colors.secondary).toBe("#5B2D8E");
      });

      it("package.json pinnou Next 14 + React 18 + Tailwind 3 (evita quebra de react-pageflip)", () => {
        const pkg = JSON.parse(
          readFileSync(path.resolve(__dirname, "../../package.json"), "utf-8")
        );
        expect(pkg.dependencies.next).toBe("14.2.21");
        expect(pkg.dependencies.react).toBe("18.3.1");
        expect(pkg.dependencies["react-dom"]).toBe("18.3.1");
        expect(pkg.devDependencies.tailwindcss).toMatch(/^\^3\./);
      });
    });
    ```
    5) Em `package.json`, adicionar dentro de `"scripts"` (DEPOIS de `"db:seed"`, mantendo vírgulas):
    ```
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui"
    ```
    6) Rodar `npm run test` — DEVE passar 5 describes com todos os tests verdes.
  </action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <acceptance_criteria>
    - `vitest.config.ts` existe na raiz e contém `alias` apontando `@` para `path.resolve(__dirname, ".")`
    - `tests/setup.ts` existe
    - `tests/smoke/scaffold.test.ts` existe
    - `package.json` contém `"test": "vitest run"` exato
    - `package.json` contém `"vitest"` em devDependencies (versão 2.x)
    - `npm run test` exita 0
    - Output do test mostra pelo menos 5 tests passando em `scaffold.test.ts`
    - NÃO há nenhum test `skip`ado ou `.only`
  </acceptance_criteria>
  <done>
    Infraestrutura de testes pronta. Plans 02/03/04 podem escrever tests contra o código que já existe.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → git | Credenciais nunca podem ir para o repo público — .gitignore é a única barreira antes do commit |
| npm registry → node_modules | Deps com versões pinnedas evitam supply-chain via range flutuante |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01 | I (Information Disclosure) | .env.local / .env | mitigate | `.gitignore` com regra literal `.env.local` + teste `git check-ignore` na verificação |
| T-00-02 | T (Tampering) | package.json deps | mitigate | Versões com `^` mas lockfile npm trava exato; smoke test protege Next/React/Tailwind com versão literal |
| T-00-03 | T (Tampering) | tailwind.config tokens | mitigate | Smoke test valida paleta primary/secondary — regressão falha CI |
</threat_model>

<verification>
- `npm install` popula node_modules sem erros
- `npx prisma generate` cria `.prisma/client` dentro de node_modules
- `npm run typecheck` exita 0 (TS strict)
- `npm run build` produz `.next/` sem erros
- `npm run test` exita 0 com >= 5 tests passando
- `.gitignore` contém `.env.local`, `node_modules/`, `.next/`
</verification>

<success_criteria>
Stack Next 14 + React 18 + Tailwind 3 validada em disco; TS strict passa; Vitest instalado e rodando; .gitignore protege credenciais; smoke test bloqueia upgrade acidental de versões pinnedas.
</success_criteria>

<output>
Após completar, criar `.planning/phases/00-funda-o-guard-rails/00-01-SUMMARY.md` descrevendo:
- Versões instaladas (npm list --depth=0)
- Output de `npm run typecheck` e `npm run build`
- Lista de tests que rodaram e passaram
- Problemas encontrados no scaffold (se houver) e como foram resolvidos sem mudar versões pinnedas
</output>
