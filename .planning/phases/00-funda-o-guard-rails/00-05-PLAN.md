---
phase: 00-funda-o-guard-rails
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03, 04]
files_modified:
  - .planning/phases/00-funda-o-guard-rails/00-VERIFICATION.md
autonomous: false
requirements:
  - FND-01
  - FND-02
  - FND-03
  - FND-04
  - FND-05
  - FND-06
  - FND-07
  - FND-08
  - LGPD-05

must_haves:
  truths:
    - "pnpm/npm dev inicia Next 14 em http://localhost:3000 sem erros"
    - "npm run typecheck exita 0 em toda a codebase (strict mode)"
    - "npm run build exita 0 e produz .next/ com rotas /, /login, /cadastro"
    - "npm run test exita 0 com >= 25 tests passando em 5 arquivos: scaffold, schema, auth-guards, auth-config, sentry-scrub, whatsapp"
    - "npx prisma migrate status imprime 'Database schema is up to date!'"
    - "SELECT COUNT(*) FROM Product retorna 5 no Supabase"
    - "Human verifica /login e /cadastro renderizando no browser"
  artifacts:
    - path: ".planning/phases/00-funda-o-guard-rails/00-VERIFICATION.md"
      provides: "Relatório de verificação com output real de cada comando"
      contains: "FND-01"
  key_links:
    - from: "00-VERIFICATION.md"
      to: "cada requirement FND-01..FND-08 + LGPD-05"
      via: "checkbox com evidência (comando + output) linkando ao Plan que entregou"
      pattern: "FND-0[1-8].*✓"
---

<objective>
Executar a verificação de ponta a ponta da Fase 0: rodar TODOS os comandos de validação (typecheck, build, test, migrate status, seed count), abrir o browser no `/login` e `/cadastro` para o humano confirmar visualmente, e gerar o VERIFICATION.md com evidência por requirement.

Purpose: gate de fechamento da Fase 0. Sem este plan, não há prova consolidada de que todos os 9 requirements (FND-01..08, LGPD-05) estão entregues e funcionando juntos. O plan produz o artifact que `/gsd-verify-phase` consumiria, mas criado manualmente porque a Fase 0 é pequena e não justifica subagente.
Output: Documento `00-VERIFICATION.md` que comprova a fundação pronta para a Fase 1.
</objective>

<execution_context>
@C:/Users/ksinh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ksinh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/00-funda-o-guard-rails/00-01-SUMMARY.md
@.planning/phases/00-funda-o-guard-rails/00-02-SUMMARY.md
@.planning/phases/00-funda-o-guard-rails/00-03-SUMMARY.md
@.planning/phases/00-funda-o-guard-rails/00-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rodar suíte de verificação full e capturar evidências</name>
  <files>.planning/phases/00-funda-o-guard-rails/00-VERIFICATION.md (novo)</files>
  <read_first>
    - .planning/REQUIREMENTS.md seção "Foundation (FND)" + LGPD-05
    - .planning/phases/00-funda-o-guard-rails/00-01-SUMMARY.md
    - .planning/phases/00-funda-o-guard-rails/00-02-SUMMARY.md
    - .planning/phases/00-funda-o-guard-rails/00-03-SUMMARY.md
    - .planning/phases/00-funda-o-guard-rails/00-04-SUMMARY.md
  </read_first>
  <action>
    1) Rodar a suíte completa SEQUENCIALMENTE e capturar output de cada comando (em memória, vai para o VERIFICATION.md):
       ```
       npm run typecheck
       npm run build
       npx dotenv -e .env.local -- npm run test
       npx dotenv -e .env.local -- npx prisma migrate status
       npx dotenv -e .env.local -- npx prisma db execute --stdin <<< "SELECT COUNT(*)::int AS n FROM \"Product\";"
       ```
       (Windows: se `<<<` falhar, criar `tmp-count.sql` com a query e rodar com `--file=tmp-count.sql`.)

    2) Iniciar `npm run dev` em background (ou pedir ao humano — Task 2 trata do visual), capturar primeira linha "ready - started server on 0.0.0.0:3000" e matar o processo após confirmação.

    3) Criar `.planning/phases/00-funda-o-guard-rails/00-VERIFICATION.md` com EXATAMENTE este template, preenchido com outputs reais capturados:

    ```markdown
    # Phase 0 — Verification Report

    **Phase:** 00-funda-o-guard-rails
    **Generated:** <ISO-8601 timestamp>
    **Requirements:** FND-01, FND-02, FND-03, FND-04, FND-05, FND-06, FND-07, FND-08, LGPD-05

    ## Requirements Coverage

    | Req | Description | Plan | Evidence | Status |
    |-----|-------------|------|----------|--------|
    | FND-01 | Next.js 14 + TypeScript estrito + Tailwind 3.4 | 00-01 | `npm run typecheck` exit 0 + `npm run build` exit 0 + smoke test versões pinnedas | ✓ |
    | FND-02 | Prisma + Supavisor pooler (6543/5432) | 00-02 | `.env.local` tem DATABASE_URL:6543?pgbouncer + DIRECT_URL:5432; `prisma migrate status` up to date | ✓ |
    | FND-03 | Schema completo + migration aplicada | 00-02 | `prisma/migrations/*_init/migration.sql` contém 10 tabelas + UNIQUE paymentId | ✓ |
    | FND-04 | Auth.js v5 email/senha + Google | 00-03 | Rotas /login e /cadastro compiladas no build; tests auth-config cobrem callback; Google button condicional em AUTH_GOOGLE_ID | ✓ |
    | FND-05 | Middleware + requireAuth/requireAdmin | 00-03 | Tests de auth-guards (6 casos) + auth-config (11 casos) passando | ✓ |
    | FND-06 | Interface `lib/whatsapp.ts` trocável | 00-04 | Test "swap Evolution → Cloud API" passa; setWhatsAppClient implementa contrato | ✓ |
    | FND-07 | Sentry beforeSend scrub PII | 00-04 | 10 tests sentry-scrub passando; 3 configs (server/client/edge) usam lib/sentry-scrub.ts | ✓ |
    | FND-08 | Seed 5 produtos com preços brief | 00-02 | Test schema.test.ts: count=5 + 4 adicionais somam 339.60 + cada slug/preço individualmente validado | ✓ |
    | LGPD-05 | Scrub PII (fotos/telefones/CPF) | 00-04 | Mesmo conjunto de tests FND-07; campos photos, photoKeys, cpf, password DROP; phone masked | ✓ |

    ## Command Outputs

    ### npm run typecheck
    ```
    <colar output — deve ser SILENCIOSO + exit 0>
    ```

    ### npm run build
    ```
    <colar output — deve listar /, /login, /cadastro, /api/auth/[...nextauth]>
    ```

    ### npm run test
    ```
    <colar output — total de tests + suites; deve ser >= 25 tests em 5 arquivos>
    ```

    ### prisma migrate status
    ```
    <colar output — deve dizer "Database schema is up to date!">
    ```

    ### SELECT COUNT(*) FROM "Product"
    ```
    <colar resultado — deve ser 5>
    ```

    ### npm run dev (startup)
    ```
    <colar primeira saída — ex: "▲ Next.js 14.2.21 — Local: http://localhost:3000 — ready in Xs">
    ```

    ## Must-Haves Check

    | Truth | Verified? |
    |-------|-----------|
    | Stack Next 14 + React 18 + Tailwind 3 pinnedas | ✓ via scaffold.test.ts |
    | Migration aplicada, 10 tabelas no Supabase | ✓ via migrate status |
    | 5 produtos seedados com preços brief | ✓ via schema.test.ts |
    | Auth Email/Senha compila + test callback | ✓ via build + auth-config.test.ts |
    | Guards em 3 camadas com test | ✓ via auth-guards.test.ts |
    | lib/whatsapp.ts trocável via setClient | ✓ via whatsapp.test.ts |
    | Sentry scrub PII em 3 runtimes | ✓ via sentry-scrub.test.ts |
    | .env.local git-ignored | ✓ via .gitignore + git check-ignore |

    ## Gaps & Known Deferrals (não-bloqueantes para Fase 0)

    - **Promover usuário para ADMIN**: no MVP, via SQL no Supabase SQL Editor — `UPDATE "User" SET role='ADMIN' WHERE email='<você>'`. Fase 6 pode adicionar CLI.
    - **Recuperação de senha**: não está em Fase 0. Endereçado na Fase 6 (Área do Cliente).
    - **Verificação de e-mail**: deferida (não está em requirements v1).
    - **Sentry DSN real em dev**: opcional. Se `SENTRY_DSN` estiver vazia em `.env.local`, os 3 configs não inicializam — sem erro. Plans 04 não exige DSN real para testes (são unit tests da função pura scrubPII).

    ## Next Phase

    Fase 1 (Site Institucional) pode começar. Pré-requisitos atendidos:
    - Layout root com fontes (Playfair + Inter + Dancing Script)
    - Tailwind tokens (primary #D97B2B, secondary #5B2D8E) prontos
    - Auth UI funcionando (Header poderá incluir "Entrar" link já)
    - DB seedado (página /produtos poderá ler do banco desde o dia 1)

    ---
    *Gerado no fim da execução do Plan 05 (Fase 0).*
    ```

    4) Se qualquer comando retornou erro ou número de tests esperado não bate:
       - NÃO preencher com "✓" enganando
       - Marcar "✗" + descrever o gap
       - Abortar este plan e sinalizar retorno ao plan que falhou
  </action>
  <verify>
    <automated>test -f .planning/phases/00-funda-o-guard-rails/00-VERIFICATION.md && grep -q "FND-08.*✓" .planning/phases/00-funda-o-guard-rails/00-VERIFICATION.md && grep -q "LGPD-05.*✓" .planning/phases/00-funda-o-guard-rails/00-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `.planning/phases/00-funda-o-guard-rails/00-VERIFICATION.md` existe
    - Contém tabela com 9 linhas, UMA por requirement (FND-01..08 + LGPD-05)
    - Cada linha tem coluna "Evidence" preenchida com comando + resultado esperado
    - Cada linha tem status `✓` (requerido para fechar fase) — se algum for `✗`, o plan retorna para revisão
    - Contém output REAL (não placeholder) de `npm run test`, `npm run build`, `prisma migrate status`, `SELECT COUNT(*) FROM "Product"`
    - Seção "Gaps & Known Deferrals" lista pelo menos: promoção admin via SQL, recuperação de senha
    - `npm run typecheck` exita 0 (re-validado aqui)
    - `npm run test` mostra >= 25 tests passando
  </acceptance_criteria>
  <done>
    Evidência consolidada — qualquer auditoria futura (compliance LGPD, handoff para outro dev, retrospectiva) consulta UM arquivo e vê fundação ok.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [HUMAN] Verificação visual no browser de /login e /cadastro</name>
  <read_first>
    - .planning/phases/00-funda-o-guard-rails/00-VERIFICATION.md (relatório gerado no Task 1 — confirmar que Task 1 rodou antes deste checkpoint)
    - app/(auth)/login/page.tsx (ver componentes renderizados e a cor primária esperada do botão)
    - app/(auth)/cadastro/page.tsx (ver 4 campos e link voltando para /login)
  </read_first>
  <what-built>
    Rotas de auth UI foram criadas (Plan 03), o servidor compila e os tests passam — mas precisamos do olho humano confirmando que renderizam no browser. É uma checagem de 60 segundos que pega o que unit test não pega: tailwind aplicado, fontes carregando, shell funcional.
  </what-built>
  <how-to-verify>
    1) Em um terminal, rodar:
       ```
       npx dotenv -e .env.local -- npm run dev
       ```
    2) Abrir http://localhost:3000/login no Chrome/Firefox.
       - Confirme: shell branco centralizado, logo "Era Uma Vez Eu" em fonte serif laranja (Playfair)
       - Confirme: form com campos "E-mail" e "Senha", botão "Entrar" laranja (#D97B2B), link "Criar conta"
       - Se `AUTH_GOOGLE_ID` estiver setado no `.env.local`: botão "Entrar com Google" aparece abaixo. Se não: botão NÃO aparece.
    3) Abrir http://localhost:3000/cadastro.
       - Confirme: form com 4 campos (nome, e-mail, senha, confirmar senha), botão "Criar conta", link voltando para /login.
    4) (Opcional, smoke final) Submeter /cadastro com email+senha reais:
       - Deve criar um User na tabela `User` do Supabase (confirmar em Supabase → Table Editor → User)
       - Password armazenada é bcrypt hash (começa com `$2a$10$` ou `$2b$10$`), NÃO em plaintext.
    5) Abrir http://localhost:3000/admin no browser (sem estar logado como ADMIN):
       - ANTES de logar: middleware redireciona para `/login?callbackUrl=%2Fadmin` (FND-05 em ação)
       - DEPOIS de logar como CUSTOMER: como ainda não existe o layout admin real (vem na Fase 6), veja que o middleware bloqueia antes mesmo de chegar no layout — URL vira /login?callbackUrl=%2Fadmin
    6) Matar o dev server (Ctrl+C).
  </how-to-verify>
  <resume-signal>
    Digite "approved" se tudo acima funcionou, ou descreva o problema (ex: "fonte Dancing Script não carregou", "botão Google apareceu sem AUTH_GOOGLE_ID") para correção antes de fechar a fase.
  </resume-signal>
  <files>(nenhum arquivo modificado — verificação visual)</files>
  <action>Human-only: rodar  e verificar /login, /cadastro no browser conforme how-to-verify. Confirmar tokens visuais (cor primária #D97B2B, fontes Playfair/Dancing), botão Google condicional, e middleware bloqueando /admin para anônimos.</action>
  <verify>(manual) Humano confirma no browser ou descreve regressão.</verify>
  <acceptance_criteria>
    - `/login` renderiza sem erro no browser (HTTP 200; DevTools → Network mostra document status 200 para /login)
    - Botão primário "Entrar" tem cor `#D97B2B` — inspecionar CSS computed no DevTools (Elements → botão → Computed → background-color em formato rgb(217, 123, 43) que equivale a #D97B2B)
    - Sem `AUTH_GOOGLE_ID` setado em `.env.local`, botão "Continuar com Google" (ou "Entrar com Google") NÃO renderiza no DOM de /login — confirmar via DevTools (Elements → busca por texto "Google" retorna 0)
    - Acesso anônimo a `/admin` redireciona para `/login?callbackUrl=%2Fadmin` — confirmar URL final no navegador
    - `/cadastro` renderiza form com campos name, email, password (e confirmar senha) — conferir `<input name="name">`, `<input name="email">`, `<input name="password">` via DevTools
  </acceptance_criteria>
  <done>Usuário responde "approved" ou desenvolvedor corrige o problema relatado e Task 2 é re-executada.</done>
</task>

</tasks>

## Known Deferrals

- **Adversarial Playwright test para Server Action admin sem auth**: será adicionado na Fase 6 quando surgir a primeira Server Action admin protegida (ADM-06 — operações admin sobre pedidos). Na Fase 0 ainda não existe Server Action admin real — o middleware `/admin` já é coberto pelo checkpoint visual (Task 2 item 5) + unit tests de `requireAdmin` do Plan 03.
  - Justificativa: criar adversarial test agora exigiria criar uma Server Action stub só para testar, o que introduz código morto. A cobertura virá naturalmente quando a Server Action real existir.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification report → next phase | VERIFICATION.md é a única fonte de verdade consultada antes de iniciar Fase 1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-17 | R (Repudiation) | Claim de "fase pronta" sem evidência | mitigate | 00-VERIFICATION.md exige comando + output — não aceita apenas "✓" sem log |
| T-00-18 | T (Tampering) | Test flaky mascarando falha de guard | mitigate | checkpoint humano visual + auth-config.test.ts determinístico (input/output puros) |
</threat_model>

<verification>
- 00-VERIFICATION.md existe e tem 9 linhas na tabela Requirements Coverage
- Todos os 9 requirements estão com status ✓
- Human aprovou /login e /cadastro no browser
- npm run test mostra ≥ 25 tests passando em todas as Waves
- Nenhum SUMMARY.md dos plans anteriores está faltando
</verification>

<success_criteria>
Phase 0 FECHADA com evidência consolidada. Qualquer pessoa abrindo `.planning/phases/00-funda-o-guard-rails/00-VERIFICATION.md` entende em 30 segundos o estado da fundação e sabe o que vem na Fase 1.
</success_criteria>

<output>
Criar `.planning/phases/00-funda-o-guard-rails/00-05-SUMMARY.md`:
- Link para o 00-VERIFICATION.md
- Resumo: 5 plans, 3 waves, 9 requirements, X tests passando
- Lessons learned (que não cabem no VERIFICATION): o que foi mais lento? o que surpreendeu? (para alimentar RETROSPECTIVE.md futura)
- Confirmação de que a Fase 1 pode iniciar
</output>
</content>
</invoke>